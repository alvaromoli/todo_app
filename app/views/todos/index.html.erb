<% @todos.each do |todo| %>
<div class="card">
  <div class="card-block">
    <p class="card-text"><%= todo.description %></p>
    <a href="#" class="btn btn-primary">Button</a>
  </div>
</div>
<% end %>
